// =============================================================================
//  secrets.h.example — modelo de credenciais do firmware
//
//  1. Copie este arquivo para "secrets.h" (na mesma pasta)
//  2. Preencha os valores reais da sua rede Wi-Fi e do seu cluster HiveMQ Cloud
//  3. "secrets.h" NÃO é versionado (está no .gitignore) — nunca faça commit dele
// =============================================================================

#pragma once

// ─── Wi-Fi ────────────────────────────────────────────────────────────────────
const char *WIFI_SSID = "SUA_REDE_WIFI";
const char *WIFI_PASSWORD = "SUA_SENHA_WIFI";

// ─── HiveMQ Cloud ─────────────────────────────────────────────────────────────
// Dashboard → Clusters → seu cluster → Overview
const char *MQTT_HOST = "SEU_CLUSTER.s1.eu.hivemq.cloud";
const int MQTT_PORT = 8883; // TLS
const char *MQTT_USER = "SEU_USUARIO_HIVEMQ";
const char *MQTT_PASSWORD = "SUA_SENHA_HIVEMQ";
